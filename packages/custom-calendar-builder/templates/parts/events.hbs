<section class="tab scrollable {{tab.cssClass}}" data-group="{{tab.group}}" data-tab="{{tab.id}}">
  {{> coming-soon-banner}}

  <div class="form-group">
    <label>Calendar Events</label>
    <div class="form-fields">
      <button type="button" class="button" disabled>
        <i class="fas fa-plus"></i> Add Event
      </button>
    </div>
    <p class="hint">Define recurring holidays, festivals, and important dates</p>
  </div>

  <fieldset>
    <legend>Event Types</legend>
    <p class="hint" style="margin-bottom: 1rem;">
      Events can be annual holidays (same date each year), lunar celebrations (tied to moon phases),
      seasonal festivals (tied to solstices/equinoxes), or custom recurring patterns.
    </p>
  </fieldset>

  <fieldset>
    <legend>Event List (Coming Soon)</legend>
    <p class="hint" style="margin-bottom: 1rem;">Each event will have: Name, Date/Pattern, Description, Category (religious, civic, seasonal, etc.), and optional cultural significance</p>

    <div style="padding: 2rem; text-align: center; color: var(--color-text-dark-secondary); background: var(--color-bg-option); border-radius: 4px; border: 1px dashed var(--color-border-light-tertiary);">
      <i class="fas fa-star fa-3x" style="margin-bottom: 1rem; opacity: 0.5;"></i>
      <p style="margin: 0;">Event editor coming soon</p>
      <p style="margin: 0.5rem 0 0 0; font-size: 0.9em;">You'll be able to create holidays, festivals, and recurring calendar events</p>
    </div>
  </fieldset>
</section>
